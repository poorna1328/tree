<!DOCTYPE html>
<html lang="en">
<head>
  <h1>suprathika deekonda</h1>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Merry Christmas Tree</title>
    <style>
        body {
            margin: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(to bottom, #0f2027, #203a43, #2c5364);
            overflow: hidden;
        }

        .tree {
            width: 0;
            height: 0;
            border-left: 150px solid transparent;
            border-right: 150px solid transparent;
            border-bottom: 300px solid #0f380f;
            position: relative;
        }

        .tree::before {
            content: "";
            position: absolute;
            width: 0;
            height: 0;
            border-left: 100px solid transparent;
            border-right: 100px solid transparent;
            border-bottom: 200px solid #0f380f;
            top: 80px;
            left: -100px;
        }

        .tree::after {
            content: "";
            position: absolute;
            width: 0;
            height: 0;
            border-left: 50px solid transparent;
            border-right: 50px solid transparent;
            border-bottom: 100px solid #0f380f;
            top: 180px;
            left: -50px;
        }

        .trunk {
            width: 40px;
            height: 60px;
            background: #8b4513;
            position: absolute;
            bottom: -60px;
            left: -20px;
        }

        .star {
            width: 0;
            height: 0;
            border-left: 25px solid transparent;
            border-right: 25px solid transparent;
            border-bottom: 50px solid gold;
            position: absolute;
            top: -50px;
            left: -25px;
            animation: twinkle 1s infinite alternate;
        }

        .ornament {
            width: 20px;
            height: 20px;
            border-radius: 50%;
            position: absolute;
            animation: twinkle 0.5s infinite alternate;
        }

        .ornament:nth-child(1) { background: red; top: 50px; left: -50px; }
        .ornament:nth-child(2) { background: blue; top: 100px; right: -30px; }
        .ornament:nth-child(3) { background: yellow; top: 150px; left: -20px; }
        .ornament:nth-child(4) { background: purple; top: 200px; right: -60px; }

        .snow {
            width: 10px;
            height: 10px;
            background: white;
            border-radius: 50%;
            position: absolute;
            top: -10px;
            animation: snowfall 5s linear infinite;
        }

        @keyframes twinkle {
            from { opacity: 0.5; }
            to { opacity: 1; }
        }

        @keyframes snowfall {
            to {
                transform: translateY(100vh);
            }
        }

        /* Create multiple snowflakes */
        .snow:nth-child(2n) { left: 20%; animation-delay: 1s; }
        .snow:nth-child(3n) { left: 40%; animation-delay: 2s; }
        .snow:nth-child(4n) { left: 60%; animation-delay: 3s; }
        .snow:nth-child(5n) { left: 80%; animation-delay: 4s; }
    </style>
</head>
<body>
    <div class="tree">
        <div class="star"></div>
        <div class="ornament"></div>
        <div class="ornament"></div>
        <div class="ornament"></div>
        <div class="ornament"></div>
        <div class="trunk"></div>
    </div>
    <div class="snow"></div>
    <div class="snow"></div>
    <div class="snow"></div>
    <div class="snow"></div>
    <div class="snow"></div>
</body>
</html>

<